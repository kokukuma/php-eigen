// $Id$
// vim:ft=javascript

// If your extension references something external, use ARG_WITH
// ARG_WITH("eigen", "for eigen support", "no");

// Otherwise, use ARG_ENABLE
// ARG_ENABLE("eigen", "enable eigen support", "no");

if (PHP_EIGEN != "no") {
	EXTENSION("eigen", "eigen.c");
}

